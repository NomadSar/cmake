#include <iostream>
#include <vector>
#include <string>
#include <fstream>
#include <limits>
#include "disk.h"


 void write(std::vector<double>& vector_int) {
	save(vector_int);
};
 void read(std::vector<double>& vector_int) {
	load(vector_int);
};



